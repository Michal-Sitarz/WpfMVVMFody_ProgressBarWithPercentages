<Window x:Class="TestMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TestMVVM"
        mc:Ignorable="d"
        Title="MainWindow" Height="200
        " Width="400"
        Background="Gray">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        
        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="1">
            <TextBlock Text="{Binding ProgressStatusInPercents}" Margin="20 20 0 20" Width="60"></TextBlock>
            <TextBlock  Margin="5 20 20 20">%</TextBlock>
        </StackPanel>

        <ProgressBar Grid.Column="1" Grid.Row="1" Height="12" Width="180" Value="{Binding ProgressStatus}"
                     Minimum="0" Maximum="{Binding Max}"/>


        <Button Click="BtnGo_Click" Grid.Row="2" Grid.Column="1" Width="100" Height="30">Go!</Button>

    </Grid>
</Window>
